- name: Deploy to App Script & Update Live Site
        id: deploy_step
        run: |
          clasp push --force
          clasp deploy -i AKfycbxgoannrkzyrAaIF8FeJP-ZJyFUrhdtT1d_iJdrY0JiJwqebPYbToS5r-nUYp6Ow-2fYw -d "GitHub Actions Auto Update"

      # 배포 실패 시에만 실행되는 단계
      - name: If Deploy Fails
        if: failure()
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: "🚨 GAS 배포 실패 경고!",
              body: "GitHub Actions에서 clasp deploy 중 오류가 발생했습니다. 로그를 확인해 주세요! 🕵️‍♂️"
            })